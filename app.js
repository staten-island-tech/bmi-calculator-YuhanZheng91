const height =
function getBMI(){
    
}

function checkBMI(){

}

function results(){
    //get wavlues for weight and height
    //get BMI

    let BMI= getBMI(height, weight);
    checkBMI(BMI);
}

// === checks type / == does not check for type